import { GetterTree } from 'vuex';
import { NoteState, RootState } from '@/store/types';

const getters: GetterTree<NoteState, RootState> = {
  category: (state: NoteState) => {
    // TODO
  },
  categoryById: (state: NoteState) => {
    // TODO
  },
  note: (state: NoteState) => {
    // TODO
  },
  noteById: (state: NoteState) => {
    // TODO
  },
};

export default getters;
